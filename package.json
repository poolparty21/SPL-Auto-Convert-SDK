{
  "name": "spl-auto-convert-sdk",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "SPL Auto-Convert SDK — automatically convert SOL to USDT",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean",
    "version-packages": "changeset version",
    "release": "changeset publish"
  },
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=11.0.0"
  },
  "packageManager": "pnpm@11.5.0",
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "unbuild": "^2.0.0"
  }
}
